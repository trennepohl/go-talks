package main

import "fmt"

func main() {
	alphabet := []string{"a", "b", "c", "d", "e"}

	//Prints the first element of the slice, arrays is the same thing as a slice
	fmt.Println(alphabet[0])

	//Prints the second element of the slice, arrays is the same thing as a slice
	fmt.Println(alphabet[1])
}
